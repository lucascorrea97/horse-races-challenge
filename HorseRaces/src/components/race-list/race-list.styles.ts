// Utils
import { createThemedStyles } from '../../utils/createThemedStyles'

const styles = createThemedStyles({
  emptyListContainer: {
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 'XL',
  },
  emptyListNoResultsText: {
    color: 'noResultsFoundTextColor',
  },
})

export default styles
