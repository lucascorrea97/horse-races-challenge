// Utils
import { createThemedStyles } from '../../utils/createThemedStyles'

const styles = createThemedStyles({
  container: {
    padding: 'S',
    backgroundColor: 'categoriesBackgroundColor',
    borderBottomColor: 'categoriesBorderColor',
    borderBottomWidth: 1,
  },
  radioPillWrapper: {
    marginRight: 'XXXS',
  },
  raceFilterText: {
    marginBottom: 'S',
  },
})

export default styles
